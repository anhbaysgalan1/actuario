
/*
import { createSelector, Selector } from 'reselect';
import { get } from 'lodash';

import { ActuarioState, Goals } from '../types/state';
import { Recipe } from '../types/factorio';

const goalsSelector: Selector<ActuarioState, Goals> = state => state.user.factory.goals;

function recipeSelector(recipeName: string): Selector<ActuarioState, Recipe | undefined> {
  return state => get(state, `data.factorio.recipes.${recipeName}`);
}

function factoryManifest
*/
